# 参与开发

请描述复现步骤、系统版本和预期行为。使用虚构名单和合成图片复现问题；不要提交真实学生姓名、学号、课次记录、现场照片或签名密钥。

提交前运行 README 中的测试和 `python tools/audit_public.py`，检查 `git diff --cached`。识别规则变更应说明适用场景及失败案例，合成图片上的测试不等同于真实场景准确率。

贡献代码遵循项目 Apache-2.0 许可。新增依赖请保留其许可说明。
